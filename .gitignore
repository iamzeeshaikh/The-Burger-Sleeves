# dependencies
node_modules/

# build output
dist/
.vercel/
.astro/
.netlify/

# secrets — never commit
.env
.env.*
!.env.example

# Private WordPress exports: SQL dumps, XML/CSV exports, backups, user records.
# These live in the PARENT folder (outside this repo) and must never enter it.
*.sql
*.sql.gz
*.sql.zip
*.xml
*.csv
# …but the migration audit CSVs in docs/ are deliverables, not exports.
!docs/*.csv
!public/**/*.xml
localhost.sql
theburgersleeves.WordPress*.xml
wc-product-export-*.csv
_migration-work/

# generated derivatives (rebuilt by `npm run og`)
public/og/

# os / editor
.DS_Store
Thumbs.db
*.log
npm-debug.log*
.idea/
.vscode/
.vercel
