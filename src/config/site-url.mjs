// Canonical production origin. Must match the hostname the live WordPress site
// canonicalises to (apex, HTTPS, no www) so canonicals and the sitemap are stable.
export const SITE_URL = 'https://theburgersleeves.com';
