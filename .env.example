# SMTP transport for the enquiry / quote forms (src/pages/api/inquiry.ts).
# Copy to .env and fill in real values. Never commit .env.
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
SMTP_TO=
SMTP_FROM_NAME=
SMTP_FROM_EMAIL=
